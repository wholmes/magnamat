# Optional: copy to `.env.docker` for `npm run db:migrate:deploy:local` / `db:seed:local` only.
# Match the host port from `.env` POSTGRES_HOST_PORT / DATABASE_URL (default 5434).
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5434/magnamat?schema=public"
